<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head> 
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="class">
            <h1 class="text-white h-full text-4xl p-10 px-10">The Flash cards🗒️</h1>
        </div>
        <div class="form p-10">
          <form action="/create" method="post">
              <input class="block w-full text-white  outline-none rounded-lg mt-2 px-5 py-5 bg-zinc-800" type="text" placeholder="Title goes Here" name="title">
              <textarea  class="block w-full text-white outline-none rounded-lg  mt-2 resize-none  w-full  px-5 py-5 bg-zinc-800 "placeholder="Write your task details"  name="details"></textarea>
              <input class="mt-2 bg-blue-900 px-3 py-2 rounded-md text-white"  type="submit" value="create task">
          </form>
        </div>
        <div class="tasks flex flex-wrap gap-4 p-10">
              <% if(files.length > 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="text-white-800 task w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%=val %></h1>
                        <div class="flex w-full justify-between items-center text-blue-500 mt-3">
                            <a class="text-blue-500 inline-block" href="/files/<%=val%>">Read More.</a>
                            <a href="/edit/<%=val%>">Edit Card.</a>
                            
                        </div>
                      <!-- Updated anchor tag with onclick event -->
                      <a class="text-yellow-100" href="/delete/<%= val %>">Delete Card</a>
                    </div>
                <% }); %>
            <% } else { %>
                <h1 class="text-white">No Tasks Yet.</h1> 
            <% } %>
            
           
        
           
           <!-- <h1 class="text-white ">No Tasks Yet.</h1> -->
        </div>
  
    </div>


 
<script src="/javascripts/script.js"></script>
</body>
</html>